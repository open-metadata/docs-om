---
title: Delete a Dashboard Service
description: Delete a dashboard service by ID or name, with soft/hard delete options
sidebarTitle: Delete a Dashboard Service
api: DELETE /v1/services/dashboardServices/{id}
---

# Delete a Dashboard Service

Delete a dashboard service by ID or fully qualified name. Supports soft delete (default), hard delete, and restore operations.

## Delete by ID

<ParamField path="id" type="string" required>
  UUID of the dashboard service to delete.
</ParamField>

<ParamField query="recursive" type="boolean" default="false">
  Recursively delete child entities (dashboards, charts).
</ParamField>

<ParamField query="hardDelete" type="boolean" default="false">
  Permanently delete the dashboard service. If `false`, the service is soft-deleted and can be restored.
</ParamField>

## Delete by Name

Use `DELETE /v1/services/dashboardServices/name/{fqn}` to delete by fully qualified name.

<ParamField path="fqn" type="string" required>
  Fully qualified name of the dashboard service (e.g., `sample_looker`).
</ParamField>

<ParamField query="recursive" type="boolean" default="false">
  Recursively delete child entities.
</ParamField>

<ParamField query="hardDelete" type="boolean" default="false">
  Permanently delete the dashboard service.
</ParamField>

## Restore a Soft-Deleted Dashboard Service

Use `PUT /v1/services/dashboardServices/restore` to restore a soft-deleted dashboard service.

<ParamField body="id" type="string" required>
  UUID of the soft-deleted dashboard service to restore.
</ParamField>

<RequestExample dropdown>
```python DELETE /v1/services/dashboardServices/{id}
from metadata.sdk import configure
import requests

configure(
    host="https://your-company.open-metadata.org/api",
    jwt_token="your-jwt-token"
)

# Soft delete by ID
response = requests.delete(
    "https://your-company.open-metadata.org/api/v1/services/dashboardServices/2c0c7c05-d820-4bea-a471-047deb7f92fd",
    headers={"Authorization": "Bearer your-jwt-token"}
)

# Hard delete
response = requests.delete(
    "https://your-company.open-metadata.org/api/v1/services/dashboardServices/2c0c7c05-d820-4bea-a471-047deb7f92fd?hardDelete=true",
    headers={"Authorization": "Bearer your-jwt-token"}
)

# Delete by name
response = requests.delete(
    "https://your-company.open-metadata.org/api/v1/services/dashboardServices/name/sample_looker",
    headers={"Authorization": "Bearer your-jwt-token"}
)

# Restore
response = requests.put(
    "https://your-company.open-metadata.org/api/v1/services/dashboardServices/restore",
    headers={"Authorization": "Bearer your-jwt-token"},
    json={"id": "2c0c7c05-d820-4bea-a471-047deb7f92fd"}
)
```

```java DELETE /v1/services/dashboardServices/{id}
import static org.openmetadata.sdk.fluent.DashboardServices.*;

// Soft delete
DashboardServices.delete("2c0c7c05-d820-4bea-a471-047deb7f92fd").execute();

// Hard delete
DashboardServices.delete("2c0c7c05-d820-4bea-a471-047deb7f92fd")
    .hardDelete()
    .execute();

// Delete by name
DashboardServices.deleteByName("sample_looker").execute();

// Restore
DashboardServices.restore("2c0c7c05-d820-4bea-a471-047deb7f92fd");
```

```bash DELETE /v1/services/dashboardServices/{id}
# Soft delete by ID
curl -X DELETE "{base_url}/api/v1/services/dashboardServices/2c0c7c05-d820-4bea-a471-047deb7f92fd" \
  -H "Authorization: Bearer {access_token}"

# Hard delete
curl -X DELETE "{base_url}/api/v1/services/dashboardServices/2c0c7c05-d820-4bea-a471-047deb7f92fd?hardDelete=true" \
  -H "Authorization: Bearer {access_token}"

# Delete by name
curl -X DELETE "{base_url}/api/v1/services/dashboardServices/name/sample_looker" \
  -H "Authorization: Bearer {access_token}"

# Restore soft-deleted dashboard service
curl -X PUT "{base_url}/api/v1/services/dashboardServices/restore" \
  -H "Authorization: Bearer {access_token}" \
  -H "Content-Type: application/json" \
  -d '{"id": "2c0c7c05-d820-4bea-a471-047deb7f92fd"}'
```
</RequestExample>

<ResponseExample>
```json Response (Soft Delete)
{
  "id": "2c0c7c05-d820-4bea-a471-047deb7f92fd",
  "name": "sample_looker",
  "fullyQualifiedName": "sample_looker",
  "serviceType": "Looker",
  "version": 0.2,
  "updatedAt": 1769982621219,
  "updatedBy": "admin",
  "deleted": true,
  "owners": [],
  "tags": [],
  "domains": []
}
```
</ResponseExample>

---

## Returns

Soft delete returns the dashboard service object with `deleted: true`. Hard delete returns no content (204). Restore returns the restored dashboard service object.

---

## Error Handling

| Code | Error Type | Description |
|------|-----------|-------------|
| `401` | `UNAUTHORIZED` | Invalid or missing authentication token |
| `403` | `FORBIDDEN` | User lacks permission to delete this dashboard service |
| `404` | `NOT_FOUND` | Dashboard service with given ID or FQN does not exist |
